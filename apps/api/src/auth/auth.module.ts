import { Module } from "@nestjs/common";

// Autenticación local (email/contraseña), Google OAuth y Apple OAuth.
// Estrategias de Passport y emisión de JWT se implementan en Fase 1 (ver docs/PRD.md).
@Module({})
export class AuthModule {}
